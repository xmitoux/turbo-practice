{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "**/test", "dist", "**/*spec.ts", "vitest.config*"],
  "compilerOptions": {
    // ビルド環境(dependencies)には["vitest/globals"]がないので読み込まないようにする
    "types": []
  }
}
